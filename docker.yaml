# https://docs.docker.com/engine/security/rootless/
- name: Install docker, root
  import_playbook: root/docker.yaml

- name: Setup user subuid/subgid for rootless docker
  import_playbook: ./subuid-subgid.yaml

- name: Install docker, user
  import_playbook: user/docker.yaml
